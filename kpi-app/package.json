{
  "name": "kpi-otdela",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "KPI отдела ассистентов: YouGile + Telegram, расчёт по фактам",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0"
  }
}
