-- Веса направлений в итоговом проценте.
--
-- Раньше пять метрик усреднялись поровну, и быстрый перенос карточки
-- в «Принята» вытягивал весь KPI, даже если задача потом висела неделями.
-- Теперь главное — выполнение (сдано из взятых), потом скорость работы,
-- и только в конце скорость принятия.
--   node migrations/008_apply.cjs

INSERT INTO settings (key, value) VALUES ('metric_weights', 'done:40,work:35,t2s:15,t2a:10')
  ON CONFLICT(key) DO NOTHING;
