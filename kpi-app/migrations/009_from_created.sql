-- Время считается от постановки, «Принята» — вспомогательная стадия.
--
-- Время до старта — от «Добавлена» до «В работе»; время решения — от
-- «Добавлена» до сдачи, без пауз. «Принята» ни одно из них не
-- останавливает: принять — ещё не значит начать. Её цифра считается
-- и показывается, но в KPI не входит, чтобы не завышать результат.
-- Добавлено качество сдачи: возвраты с проверки снимают проценты.
--   node migrations/009_apply.cjs

INSERT INTO settings (key, value) VALUES ('metric_weights', 'work:40,done:30,t2s:20,quality:10')
  ON CONFLICT(key) DO UPDATE SET value = excluded.value;
